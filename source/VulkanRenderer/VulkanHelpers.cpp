#include "stdafx.h"
#include "VulkanHelpers.h"

void D3D::CreateBuffer(VkDeviceSize /*size*/, VkBufferUsageFlags /*usage*/, VkMemoryPropertyFlags /*properties*/, VkBuffer& /*buffer*/, VkDeviceMemory& /*bufferMemory*/)
{
}

void D3D::CopyBuffer(VkBuffer /*srcBuffer*/, VkBuffer /*dstBuffer*/, VkDeviceSize /*size*/)
{
}
